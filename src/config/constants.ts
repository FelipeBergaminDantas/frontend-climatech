/** ID do proprietário padrão (mock) */
export const OWNER_ID = 'user-1'

/** Chave de sessão no sessionStorage */
export const SESSION_KEY = 'climatech-session'

/** Chave de preferências de tema no localStorage */
export const STORAGE_KEY = 'climatech-theme'

/** Token 2FA mock (em produção seria TOTP) */
export const MOCK_2FA_TOKEN = '123456'
